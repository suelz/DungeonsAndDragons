package DnD.Specializations;

import DnD.Spec;

public class Monk extends Spec {
    public Monk(String specName, String desc, String hitDie, String primaryStat, String savingThrow, String weapon) {
        super(specName, desc, hitDie, primaryStat, savingThrow, weapon);
    }
}
