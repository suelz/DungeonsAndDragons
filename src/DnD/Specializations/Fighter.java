package DnD.Specializations;

import DnD.Spec;

public class Fighter extends Spec {

    public Fighter(String specName, String desc, String hitDie, String primaryStat, String savingThrow, String armor, String weapon) {
        super(specName, desc, hitDie, primaryStat, savingThrow, armor, weapon);
    }
}
