package DnD.Specializations;

import DnD.Spec;

public class Warlock extends Spec {

    public Warlock(String specName, String desc, String hitDie, String primaryStat, String savingThrow, String armor, String weapon) {
        super(specName, desc, hitDie, primaryStat, savingThrow, armor, weapon);
    }
}
